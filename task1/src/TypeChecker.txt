The type checker operates in three phases before interpretation begins. First, it builds function signatures by collecting all function declarations, checking for duplicates and validating parameter types. Second, it verifies semantic constraints like the existence of main with correct return type and argument count. Third, it type checks each function body by creating a type environment mapping variables to types, then recursively inferring expression types while validating operator requirements and checking that each function body matches its declared return type.