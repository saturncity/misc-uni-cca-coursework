int main (bool a, bool b) {
    bool and := (a & b);
    bool or := (a | b);
    bool xor := (a ^ b);
    print convert(a);
    print space;
    print convert(b);
    print space;
    print convert(and);
    print space;
    print convert(or);
    print space;
    print convert(xor);
    (convert(~a)+convert(~b))
}
int convert (bool b) {
    int n := 0;
    if b then {n := 1} else {skip};
    n
}
